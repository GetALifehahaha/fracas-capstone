import type { Operator } from '@/features/history/types/api'

const operatorsFixture: Operator[] = [
    { id: 2, name: 'Root Root' },
    { id: 3, name: 'op_staff_probe' },
    { id: 6, name: 'smoke_op' },
]

export default operatorsFixture
